#ifndef READWORD_H
#define READWORD_H

/*Reads the words entered by the user and stores them in a dynamically
    allocated string. Skips any Space before the word
    and Stops when it encounters a white space character*/
char *read_char(void);

#endif